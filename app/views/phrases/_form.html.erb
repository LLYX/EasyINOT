<%= form_for(@phrase) do |f| %>
  <% if @phrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phrase.errors.count, "error") %> prohibited this phrase from being saved:</h2>

      <ul>
      <% @phrase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :english_phrase %><br>
    <%= f.text_area :english_phrase, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :french_phrase %><br>
    <%= f.text_area :french_phrase, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :version %><br>
    <%= f.text_field :version, :class => "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
